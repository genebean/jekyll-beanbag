# Hugo output
public/

# Hugo resource cache — _gen/ is committed so Netlify skips image reprocessing
resources/*
!resources/_gen/

# Hugo temp files
.hugo_build.lock

# Nix direnv cache
.direnv/

# Editor and tool artifacts
.vscode/
.vagrant/
.claude/
node_modules/
